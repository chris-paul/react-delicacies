<!--
 * @Description: eslint配置
 * @Author: 廉恒凯
 * @Date: 2019-08-24 20:27:18
 * @LastEditTime : 2019-12-27 15:47:46
 * @LastEditors  : Please set LastEditors
 -->
```javascript
{
     /* 解析我们的js的代码查看问题的解析器 */
    "parser": "babel-eslint",
    /* 指定代码的执行环境,这样就可以使用window */
    "env": {
      "browser": true,
      "es6": true,
      "node": true
    },
    /* 全局模式 */
    "globals": {
        "__dirname": true
    },
    /* babel-eslint参数,ES6,文件类型是模块化的 */
    "parserOptions": {
      "ecmaVersion": 6,
      "sourceType": "module",
      "ecmaFeatures": {
        "jsx": true,
        "modules": true,
        "legacyDecorators": true
      }
    },
    /* 继承这个规则，下面也可以自己修改 */
    "extends": "airbnb",
    /* 支持react */
    "plugins": ["react"],
    /* 这里是所有插件共享的设置 */
    "settings": {
        /* 指定react的版本根据依赖自己识别 */
        "react": { "version": "detect" }
    },
    "rules": {
        /* 分号不需要检测 */
        "semi": [0],
        /* 重写继承的规则,js中可以出现jsx的语言 */
        "react/jsx-filename-extension": [1, { "extensions": [".js", ".jsx"] }],
        /* 数组和对象键值对最后一个逗号， never参数：不能带末尾的逗号, always参数：必须带末尾的逗号，always-multiline：多行模式必须带逗号，单行模式不能带逗号 */
        "comma-dangle": [1, "always-multiline"],
        /* 必须的四行缩紧 */
        "indent": ["error", 4, {"SwitchCase": 1}],
        "react/jsx-indent": [
            "error",
            4
        ],
        "react/jsx-indent-props": [
            "error",
            4
        ],
        /* 可以使用object.assign */
        "prefer-object-spread" : 0
        /* const RouteWithSubRoutes = (route) => () router可以不带括号 */
        "arrow-parens": 0,
        /* 不必把属性都展开 https://github.com/yannickcr/eslint-plugin-react/issues/1094 */
        "react/jsx-props-no-spreading": 0,
        /* 只有一个export必须default export */
        "import/prefer-default-export": 0,
        /* 类型检测不必须 */
        "react/prop-types": 0,
        /* 可以使用bind */
        "react/jsx-no-bind": 0,
        /* 可以使用href="javascript:;"*/
        "no-script-url": 0,
        /* 箭头函数可以直接return */
        arrow-body-style: 0,
        /* 每一行最大字符数 */
        "max-len": 0,
        /* 可以使用for -in*/
        "no-restricted-syntax": 0,
        /* 可以使用continue */
        "no-continue": 0,
        /*变量可以有_*/
        "camelcase": 0
    }
}
```
